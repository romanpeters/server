---
services:
  heimdall:
    container_name: heimdall_rtv
    image: linuxserver/heimdall:2.5.8
    environment:
      - APP_NAME=RomanTV
    volumes:
      - ./heimdall:/config
    ports:
      - ${RTV_PORT}:80
    restart: always
    labels:
      - homepage.group=RomanTV
      - homepage.name=Landing Page
      - homepage.icon=heimdall.png
      - homepage.href=https://rtv.${DOMAIN_NAME}/
