[defaults]
# Use hosts.csv-based dynamic inventory
inventory = inventory/hosts_yml.py
roles_path = roles

# Disable host key checking for dynamic environments
host_key_checking = False
local_tmp = ./.ansible/tmp
